# Lgp configuration file

[DEFAULT]
# list of Debian distributions (from images created by setup). Use 'all' for
# automatic detection or 'changelog' for the value found in debian/changelog
#distrib=unstable,stable

# specifies the location of base.tgz used by pbuilder
basetgz=/var/cache/lgp/buildd



# Lgp builder config
# Specific options are added. See lgp build --help
[LGP-BUILD]

# use an alternate setup file with Lgp expected arguments (see documentation)
#setup-file=setup.mk

# where to put compilation results
result=~/dists

# try to sign Debian package(s) just built (uncomment to enable)
#sign=yes

# run pbuilder hooks in '/etc/lgp/hooks' (uncomment to enable)
#hooks=yes

# run embedded post-treatments: add trivial repository (uncomment to enable)
#post-treatments=yes



# Lgp checker config
# Specific options are added. See lgp check --help
[LGP-CHECK]

# force the inclusion of other check functions
#include=

# force the exclusion of other check functions
#exclude=

# set a specific check functions list
#set=debian_dir,debian_rules,debian_copying,debian_changelog,package_info,readme,changelog,bin,tests_directory,setup_file,copying,homepage,builder,keyrings,announce,release_number,manifest_in,include_dirs,scripts,pydistutils,debian_maintainer,debian_uploader



# Lgp tagger class
# Specific options are added. See lgp tag --help
[LGP-TAG]

# list of tag formats to apply (Logilab policy. Feel free to change!)
format=$project-version-$version, $project-debian-version-$version-$debian_revision

#!/bin/sh
set -e
PKG=logilab-packaging
if [ "$1" = configure ] ; then
	[ ! -L /usr/share/cdebootstrap/suites ] \
	&& dpkg-divert --package ${PKG} --rename --add /usr/share/cdebootstrap/suites \
	&& ln -fvs /etc/lgp/suites /usr/share/cdebootstrap/suites
fi
#DEBHELPER#
exit 0
